import setuptools


setuptools.setup(
        name='autolearn',
        version='0.0.1',
        author='Sander Vandenhaute',
        packages=setuptools.find_packages(),
        python_requires='>=3.8',
        )
